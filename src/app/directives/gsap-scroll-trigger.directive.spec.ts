import { GsapScrollTriggerDirective } from './gsap-scroll-trigger.directive';

describe('GsapScrollTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new GsapScrollTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
